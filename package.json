{
  "name": "annagodwin-website",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio website for Anna Godwin",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "*.{html,css,js,json,md}": "prettier --write"
  }
}
